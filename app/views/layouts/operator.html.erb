<!DOCTYPE html>
<html lang="en">
  <head>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="<%= favicon(current_tenant) %>">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1, shrink-to-fit=no, user-scalable=no">
    <meta name="turbo-cache-control" content="no-cache">
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
    <script   src="https://code.jquery.com/jquery-3.4.1.min.js"   integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo="   crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>
    <% if @include_stripe %>
      <script src="https://js.stripe.com/v3/" data-turbo-track=reload></script>
      <%= render "layouts/stripe" %>
    <% end %>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.22.2/moment.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/tempusdominus-bootstrap-4/5.0.0-alpha14/js/tempusdominus-bootstrap-4.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tempusdominus-bootstrap-4/5.0.0-alpha14/css/tempusdominus-bootstrap-4.min.css" />

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-Fy6S3B9q64WdZWQUiU+q4/2Lc9npb8tCaSX9FK7E8HnRr0Jz8D6OP9dO5Vg3Q9ct" crossorigin="anonymous"></script>
    <% if @include_recaptcha %>
      <script src="https://www.google.com/recaptcha/api.js?render=<%= ENV['RECAPTCHA_SITE_KEY'] %>"></script>
    <% end %>

    <style>
      :root {
        --brand-color: <%= brand_color(current_tenant) %>;
        --brand-color-hover: <%= brand_color_hover(current_tenant) %>;
      }
      .btn-primary, .btn-primary:focus,
      .btn-info, .btn-info:focus,
      .btn-success, .btn-success:focus {
        background-color: var(--brand-color) !important;
        border-color: var(--brand-color) !important;
        color: #fff !important;
      }
      .btn-primary:hover, .btn-primary:active,
      .btn-info:hover, .btn-info:active,
      .btn-success:hover, .btn-success:active {
        background-color: var(--brand-color-hover) !important;
        border-color: var(--brand-color-hover) !important;
      }
      .btn-outline-primary {
        color: var(--brand-color) !important;
        border-color: var(--brand-color) !important;
      }
      .btn-outline-primary:hover, .btn-outline-primary:active {
        background-color: var(--brand-color) !important;
        color: #fff !important;
      }
    </style>
    <%= content_for?(:head) ? yield(:head) : '' %>

    <% title = yield(:title) %>
    <title><%= title || current_tenant.name %></title>
    <% set_tracking_pixels %>
    <% if @head_pixels %>
      <% @head_pixels.each do |pixel| %>
        <%= raw pixel.script %>
      <% end %>
    <% end %>
  </head>

  <% if @background_image %>
    <% if @background_image.attached? %>
      <body style="background-image: url('<%= url_for(@background_image) %>'); background-repeat: no-repeat; background-size: cover;">
    <% else %>
      <body style="background-color: #f5f5f7;">
    <% end %>
  <% else %>
    <body style="background-color: #f5f5f7;">
  <% end %>
    <%= render "layouts/nav_renderer" %>
    <div class="container-fluid" style="min-height: 100vh;">
      <%= render "layouts/checkin" %>
      <%= render "layouts/flash" %>
      <%= yield %>
    </div>

    <% if mobile_app_request? && logged_in? && @navigation.respond_to?(:tab_paths) %>
      <div style="min-height: 80px;">&nbsp;</div>
      <div style="position: fixed; bottom: 0; left: 0; right: 0; background-color: #fff; border-top: 1px solid #e0e0e0; display: flex; justify-content: space-around; align-items: center; padding-bottom: env(safe-area-inset-bottom, 0px); z-index: 1050;">
        <% tabs = @navigation.tab_paths %>
        <% has_keys_tab = tabs.any? { |t| t[:title] == "Building Access" || t[:title] == "Keys" } %>
        <% if !has_keys_tab && policy(:door).enabled? %>
          <% tabs = tabs.insert(1, {title: "Keys", path: keys_doors_path}) %>
        <% end %>
        <% tabs.each do |tab| %>
          <% icon = case tab[:title]
               when "Home" then "fas fa-home"
               when "Building Access", "Keys" then "fas fa-key"
               when "Reserve a room" then "fas fa-calendar-alt"
               when "My Account" then "fas fa-user"
               when "Search" then "fas fa-search"
               else "fas fa-circle"
             end %>
          <% label = tab[:title] == "Building Access" ? "Keys" : tab[:title] %>
          <a href="<%= tab[:path] %>" style="display: flex; flex-direction: column; align-items: center; padding: 8px 12px; text-decoration: none; color: <%= request.path == tab[:path] ? brand_color(current_tenant) : '#8e8e93' %>; font-size: 10px;" data-turbo-action="<%= restore_if_possible %>">
            <i class="<%= icon %>" style="font-size: 20px; margin-bottom: 2px;"></i>
            <%= label %>
          </a>
        <% end %>
      </div>
    <% elsif mobile_app_request? %>
      <div style="min-height: 6em;">&nbsp;</div>
    <% end %>
    <% if @body_pixels %>
      <% @body_pixels.each do |pixel| %>
        <%= pixel.script %>
      <% end %>
    <% end %>
  </body>
  <footer>
    <% if @footer_pixels %>
      <% @footer_pixels.each do |pixel| %>
        <%= raw pixel.script %>
      <% end %>
    <% end %>
  </footer>
</html>
