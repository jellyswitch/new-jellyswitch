<%= form_for(@door, remote: true) do |f| %>
  <%= render "shared/error_messages" , model: @door %>

  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.submit label, class: "form-control btn btn-primary" %>
<%- end %>
