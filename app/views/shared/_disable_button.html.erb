<script>
  $('<%= submit_id %>').attr('disabled', true);
  $('<%= text_id %>').keyup(function () {
    var disable = false;
    var text = $("<%= text_id %>").val()
    console.log(text)
    if (text  == "")
      disable = true
    $('<%= submit_id %>').prop('disabled', disable);
  });
</script>