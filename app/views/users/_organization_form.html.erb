<%= form_for(@user, url: user_update_organization_path(@user), class: "form-inline") do |f| %>
  <%= f.label :organization %>
  <%= f.select :organization_id, Organization.options_for_select, class: "form-control" %>

  <%= f.submit "Update", class: "form-control btn btn-primary btn-sm" %>
<%- end %>