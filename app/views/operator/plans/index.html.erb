
<div class="row mt-3">
  <div class="col-sm-3"></div>
  <div class="col-sm">
    <div class="jumbotron bg-white pb-2 pt-2">
      <h4 class="display-4 text-center mt-4">Membership Plans</h4>
      <%= link_to "Add a new plan", new_plan_path, class: "btn btn-primary btn-sm" %>
      <%- @plans.each do |plan| %>
        <dl class="row">
          <dt class="col-sm"><%= link_to plan.name, plan_path(plan) %></dt>
          <dd class="col-sm">
            <%= render "actions", plan: plan %>
          </dd>
        </dl> 
      <%- end %>
    </div>
  </div>
  <div class="col-sm-3"></div>
</div>
