<% title @post.title %>

<%= render "shared/back_bar", back_path: posts_path, back_label: "Bulletin Board", page_title: "Post" %>

<%= render Layout::FullWidth.new do %>
  <%= render Posts::Post.new(post: @post) %>
<% end %>