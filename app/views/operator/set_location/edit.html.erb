<% title "Locations" %>

<div class="row mt-0 mb-0">
  <div class="col-lg-3"></div>
  <div class="col-lg-6 m-0 pl-0 pr-0">
    <%= list_wrapper do %>
      <%= list_item do %>
        <h4 class="display-4 text-center mt-4">Change Location</h4>

        <% if current_location.present? %>
          <p class="text-center">Your current location is: <%= current_location.name %></p>
        <% end %>
      <% end %>
      <%# Scope to current operator + visible. `Location.visible` is a
          global scope that leaks every operator's locations into every
          operator's switch page; same bug we fixed in landing/index. %>
      <%= render current_tenant.locations.visible, label: "Switch to" %>
    <% end %>
  </div>
  <div class="col-lg-3"></div>
</div>

