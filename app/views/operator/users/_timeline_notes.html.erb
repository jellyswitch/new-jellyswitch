<% activities = user.activities.where(kind: ActivityTimelineHelper::KIND_GROUPS["notes"]).recent.limit(50) %>
<% if activities.any? %>
  <% activities.each do |activity| %>
    <%= render "timeline_activity_row", activity: activity %>
  <% end %>
<% else %>
  <p class="text-muted text-center py-3 mb-0">No notes on this person yet.</p>
<% end %>
