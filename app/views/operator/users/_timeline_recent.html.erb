<%# Door punches are hidden here (kept only at each join/payment milestone) to
    cut the noise — the full history is in the "Doors" tab. %>
<% activities = user.recent_timeline_activities(limit: 50).to_a %>
<% hours = TimelineHoursIndex.build(user: user, activities: activities) %>
<% if activities.any? %>
  <% activities.each do |activity| %>
    <%= render "timeline_activity_row", activity: activity, hours: hours %>
  <% end %>
<% else %>
  <p class="text-muted text-center py-3 mb-0">No activity yet.</p>
<% end %>
