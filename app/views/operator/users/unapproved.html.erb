<% title "Approval Queue" %>

<%= render "shared/back_bar", back_path: members_groups_path, back_label: "Members & Groups", page_title: "Approval Queue" %>

<%= wide_card do %>
  <h4 class="display-4 text-center mb-1">Approval Queue</h4>
  <p class="text-center text-muted mb-3">
    <%= @users.count %> <%= "member".pluralize(@users.count) %> waiting for approval.
  </p>

  <div class="d-flex justify-content-center mb-2">
    <div class="btn-group" role="group" aria-label="Member view">
      <%= link_to "All", users_path, class: "btn btn-sm btn-outline-secondary" %>
      <%= link_to unapproved_users_path, class: "btn btn-sm btn-primary" do %>
        Approval queue
        <% if @users.count > 0 %>
          <span class="badge badge-light ml-1"><%= @users.count %></span>
        <% end %>
      <% end %>
      <%= link_to "Archived", archived_users_path, class: "btn btn-sm btn-outline-secondary" %>
    </div>
  </div>
<% end %>

<%= wide_card do %>
  <% if @users.count < 1 %>
    <div class="text-center py-4">
      <div class="mb-3" style="font-size: 2.5rem; color: #c8ced3;">
        <i class="fas fa-check-circle"></i>
      </div>
      <h5 class="mb-2">All caught up</h5>
      <p class="text-muted small mb-3" style="max-width: 320px; margin-left: auto; margin-right: auto;">
        No members are waiting for approval. New signups will land here.
      </p>
    </div>
  <% else %>
    <div class="list-group list-group-flush">
      <% @users.each do |user| %>
        <%= render "approval_card", user: user %>
      <% end %>
    </div>
  <% end %>
<% end %>
