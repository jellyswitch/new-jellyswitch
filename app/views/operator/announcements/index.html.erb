<% title "Announcements" %>

<div class="row mt-1 mb-0 p-0">
  <div class="col-lg-3"></div>
  <div class="col-lg">
    <% if policy(Announcement.new).new? %>
      <%= link_to "Post an announcement", new_feed_item_path, class: "btn btn-block btn-primary mb-2" %>
    <% end %>

    <% if @announcements.count > 0 %>
      <%= list_wrapper do %>
        <% @announcements.each do |announcement| %>
          <%= render announcement %>
        <% end %>
      <% end %>
      
      <div class="mt-2">
        <%== pagy_bootstrap_responsive_nav(@pagy) %>
      </div>
      
    <% else %>
      <%= list_wrapper do %>
        <%= list_item do %>
          No announcements yet!
        <% end %>
      <% end %>
    <% end %>

  </div>
  <div class="col-lg-3"></div>
</div>