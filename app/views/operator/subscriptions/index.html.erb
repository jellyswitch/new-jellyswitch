<%- title "Memberships" %>

<%= render Layout::FullWidth do %>
  <%= list_item do %>
    <h4 class="display-4 text-center">
      <i class="fas fa-users"></i>
      &nbsp;
      Memberships
    </h4>
  <% end %>
  <% if current_location.plans.visible.available.count == 1 %>
    <%= list_item do %>
      <%= render(MemberOnboarding::Membership, plan: current_location.plans.visible.available.first) %>
    <% end %>
  <% else %>
    <% current_location.plans.visible.available.order(:amount_in_cents).each do |plan| %>
      <%= list_item do %>
        <%= render(MemberOnboarding::Membership, plan: plan) %>
      <% end %>
    <% end %>
  <% end %>
<% end %>