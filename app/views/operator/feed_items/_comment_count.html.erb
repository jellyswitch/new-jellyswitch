<%= link_to feed_item_path(feed_item), class: "ml-3" do %>
  <i class="far fa-comment-alt">
    <% if feed_item.feed_item_comments.count > 0 %>
      <%= feed_item.feed_item_comments.count %>
    <% end %>  
  </i>
<% end %>
