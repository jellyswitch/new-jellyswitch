<% if user.has_billing_for_location?(helpers.current_location) || user.out_of_band? %>
  <%= link_to new_operator_admin_subscription_path(user_id: user.id), class: classes do %>
    <%= content %>
  <% end %>
<% else %>
  <%= link_to "#", class: "#{classes} disabled" do %>
    Cannot add membership: No payment method on file
  <% end %>
<% end %>
