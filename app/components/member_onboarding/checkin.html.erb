<div class="row mb-2 mr-1 ml-1">
  <div class="col mt-3">
    <h5 class="text-center">
      <i class="fas fa-hourglass-start"></i>
      &nbsp;
      Check In
    </h5>
    <div class="mt-3">
      Pay as you go. You'll be charged when you check out.
    </div>
    <div class="mt-3">
      Our current rate is: &nbsp;<span class="text-success font-weight-bold"><%= hourly_rate(location) %></span>
    </div>
    <div class="mt-3">
      Pro-rated to the minute, and includes access to the building, wifi, and a flex desk. Only available during working hours.
    </div>
    <div class="d-flex justify-content-center mt-3">
      <% if has_billing %>
        <%= link_to "Check in now for #{hourly_rate(location)}", checkins_path, method: :post, remote: true, class: "btn btn-primary w-100", style: "max-width: 420px;" %>
      <% else %>
        <%= link_to "Check in now for #{hourly_rate(location)}", new_checkin_path, class: "btn btn-primary w-100", style: "max-width: 420px;" %>
      <% end %>
    </div>
  </div>
  
</div>